#include <gpu_cache.hpp>

GPUCache::GPUCache(const size_t set_size, const size_t way_size, const size_t block_size)
            :CacheBase(set_size, way_size, block_size){
    // allocate the GPUCache array, etc.            
}

void GPUCache::read(const size_t & entry_index){
    
}

void GPUCache::write(){

}